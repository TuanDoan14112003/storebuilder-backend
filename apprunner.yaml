version: 1.0
runtime: python311
run:
  runtime-version: 3.11.13
  pre-run:
    - pip3 install --upgrade pip
    - pip3 install -r requirements.txt
  command: gunicorn storebuilder.wsgi:application --bind 0.0.0.0:${PORT} --workers 3 --timeout 120
  network:
    port: 8080
